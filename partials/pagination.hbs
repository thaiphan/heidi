<div class="pagination">
  {{#if prev}}
    <a class="pagination-item older" href="{{pageUrl prev}}">Newer</a>
  {{else}}
    <span class="pagination-item older">Newer</span>
  {{/if}}
  {{#if next}}
    <a class="pagination-item newer" href="{{pageUrl next}}">Older</a>
  {{else}}
    <span class="pagination-item newer">Older</span>
  {{/if}}
</div>
