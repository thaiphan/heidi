<ul class="sidebar-nav">
  {{#foreach navigation}}
    <li class="sidebar-nav-item nav-{{slug}}{{#if current}} active{{/if}}">
      <a href="{{url absolute="true"}}">{{label}}</a>
    </li>
  {{/foreach}}
</ul>
